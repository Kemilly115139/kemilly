function setup() {
    createCanvas(500, 500);
  }
  
  function draw() {
    background("white");
  }
    function draw() {
     fill("red");
    stroke("black");
  
    
    console.log(mouseIsPressed)
    
    if (mouseIsPressed) 
      rect(mouseX,mouseY,20,35)
    }